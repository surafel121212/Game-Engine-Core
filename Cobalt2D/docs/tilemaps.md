# Tilemaps

`Tilemap` stores a grid of tile IDs and orientation metadata. It provides
brush-compatible `set`, `get`, `clear`, flood fill, and world/cell conversion
operations. A project editor can layer a brush, rectangle, erase, and selection
tool on this stable data model.